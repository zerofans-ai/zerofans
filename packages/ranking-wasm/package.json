{
  "name": "@zerofans/ranking-wasm",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg --out-name ranking_wasm",
    "clean": "rm -rf pkg"
  },
  "files": [
    "pkg"
  ]
}
